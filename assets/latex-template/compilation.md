In order to compile latex documents we suggest to use:

* lualatex
* biblatex

which are the modern counterparts of pdflatex and bibtex.

We have included a script that performs the compilation in ltx.

